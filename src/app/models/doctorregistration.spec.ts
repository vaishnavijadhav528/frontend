import { Doctorregistration } from './doctorregistration';

describe('Doctorregistration', () => {
  it('should create an instance', () => {
    expect(new Doctorregistration()).toBeTruthy();
  });
});
